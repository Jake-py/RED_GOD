# Отчет по OSINT боту

## 📌 Обзор проекта
Telegram бот для сбора открытой разведывательной информации (OSINT) по никнеймам пользователей в различных социальных сетях и платформах.

## 🚀 Функциональность
- Поиск по никнейму в 10+ популярных платформах
- Интерактивное меню с кнопками
- Асинхронная проверка платформ
- Подробный отчет о найденных аккаунтах
- Кэширование результатов

## 🛠 Технический стек
- Python 3.13
- aiogram 2.x для работы с Telegram API
- aiohttp для асинхронных HTTP-запросов
- Pydantic для валидации данных
- Loguru для логирования

## ⚙️ Установка
1. Клонируйте репозиторий:
   ```bash
   git clone <ваш-репозиторий>
   cd RED_GOD
   ```

2. Создайте и активируйте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   .\venv\Scripts\activate  # Windows
   ```

3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

4. Создайте файл `.env` и настройте переменные окружения:
   ```
   BOT_TOKEN=ваш_токен_бота
   ADMIN_IDS=[ваш_telegram_id]
   ```

## 🚀 Запуск
```bash
python main.py
```

## 🎮 Использование
- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/osint_username` - Найти аккаунты по никнейму
- `/cancel` - Отменить текущую операцию

## 📁 Структура проекта
```
RED_GOD/
├── config/
│   └── settings.py    # Настройки приложения
├── src/
│   ├── core/
│   │   └── bot.py    # Основная логика бота
│   ├── modules/
│   │   └── osint/
│   │       └── username.py  # Модуль поиска по никнейму
│   └── utils/
│       └── formatter.py     # Форматирование результатов
├── .env.example      # Пример файла с переменными окружения
├── main.py           # Точка входа
└── requirements.txt  # Зависимости
```

## 🔒 Безопасность
- Никогда не храните токены и API-ключи в коде
- Используйте виртуальное окружение
- Ограничьте доступ к боту через настройки `ADMIN_IDS`

## 📈 Планы по развитию
- [ ] Добавить поиск по email и номеру телефона
- [ ] Интеграция с дополнительными OSINT-инструментами
- [ ] Визуализация связей между аккаунтами
- [ ] Графический интерфейс для веб-версии

## 📝 Лицензия
MIT License

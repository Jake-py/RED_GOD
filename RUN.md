# 🚀 Команды для запуска бота

## Требования
- Python 3.10+
- pip (or pip3)

---

## 1️⃣ Первичная настройка (первый раз)

### Создание виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate

# На Windows используйте:
# venv\Scripts\activate

# Обновить pip
pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt
```

---

## 2️⃣ Запуск бота

### Активировать виртуальное окружение

```bash
# Linux/macOS
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### Запустить бота

```bash
python3 main.py

# Или просто:
python main.py
```

---

## 3️⃣ Полная последовательность команд (один раз)

```bash
# Клонировать/перейти в проект
cd /home/red/projects/Not_Ready/RED_GOD

# Создать venv
python3 -m venv venv

# Активировать
source venv/bin/activate

# Обновить pip
pip install --upgrade pip

# Установить зависимости (сначала попробовать с --prefer-binary для деплоя)
pip install --prefer-binary -r requirements.txt

# Или если не работает:
pip install -r requirements.txt

# Запустить бота
python3 main.py
```

---

## 4️⃣ Каждый раз при новом открытии терминала

```bash
# Перейти в папку проекта
cd /home/red/projects/Not_Ready/RED_GOD

# Активировать venv
source venv/bin/activate

# Запустить бот
python3 main.py
```

---

## ⚙️ Конфигурация

Убедитесь, что файл `.env` находится в корневой папке проекта с содержимым:

```env
BOT_TOKEN=ваш_токен_здесь
```

---

## 📋 Проверка установки

```bash
# После активации venv проверить установленные пакеты
pip list

# Проверить версию Python
python --version

# Проверить наличие .env файла
ls -la | grep .env
```

---

## 🐛 Решение проблем

### Если команда `source` не работает (Windows):
```bash
# Используйте вместо этого:
venv\Scripts\activate
```

### Если `python3` не найден (Windows):
```bash
# Используйте:
python main.py
```

### Если требуются права администратора:
```bash
# Linux/macOS
sudo python3 -m pip install -r requirements.txt
```

### Если ошибка "Read-only file system" при установке (деплой на Render и подобные):
```bash
# Используйте флаг --prefer-binary для установки готовых wheels
pip install --prefer-binary -r requirements.txt
```

---

## ✅ Успешный запуск

Если вы видите в консоли:
```
DEBUG: Token loaded: xxxxx...
🚀 Starting OSINT Bot...
```

**Бот успешно запущен!** ✨

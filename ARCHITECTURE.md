# 🏗️ Архитектура OSINT Бота

## Структура проекта

```
RED_GOD/
├── main.py                    # Точка входа
├── config/
│   └── settings.py           # Конфигурация и переменные окружения
├── src/
│   ├── core/
│   │   └── bot.py            # Основная логика бота (обработчики команд)
│   ├── modules/
│   │   └── osint/
│   │       ├── username.py   # Поиск по никнейму (новый упрощенный)
│   │       ├── scrapers.py   # Парсинг соц сетей (НОВЫЙ)
│   │       ├── phone.py      # Поиск по телефону (НОВЫЙ)
│   │       ├── email.py      # Поиск по email (НОВЫЙ)
│   │       └── domain.py     # Анализ домена (НОВЫЙ)
│   └── utils/
│       └── formatter.py      # Форматирование результатов (ОБНОВЛЕН)
├── data/
│   ├── cache/                # Кеш данных
│   └── logs/                 # Логи бота
├── requirements.txt          # Зависимости
└── .env                       # Переменные окружения
```

## Поток данных

### 1️⃣ Поиск по Username

```
User: /osint_username
  ↓
FSMContext: set waiting_for_username
  ↓
User: "john_doe"
  ↓
search_username() in username.py
  ↓
scrape_username_info() in scrapers.py
  ↓
PlatformScraper classes:
  ├── GitHubScraper
  ├── TwitterScraper
  ├── InstagramScraper
  ├── LinkedInScraper
  ├── VKScraper
  └── TelegramScraper
  ↓
format_username_detailed() in formatter.py
  ↓
Telegram: Beautiful formatted message
```

### 2️⃣ Поиск по Телефону

```
User: /osint_phone
  ↓
FSMContext: set waiting_for_phone
  ↓
User: "+7 9123456789"
  ↓
search_phone() in phone.py
  ↓
search_phone_on_sites() in phone.py
  ↓
format_phone_results() in formatter.py
  ↓
Telegram: Phone search results
```

### 3️⃣ Поиск по Email

```
User: /osint_email
  ↓
FSMContext: set waiting_for_email
  ↓
User: "user@gmail.com"
  ↓
search_email() in email.py
  ↓
get_email_domain_info() in email.py
  ↓
format_email_results() in formatter.py
  ↓
Telegram: Email results
```

### 4️⃣ Анализ Домена

```
User: /osint_domain
  ↓
FSMContext: set waiting_for_domain
  ↓
User: "example.com"
  ↓
analyze_domain_complete() in domain.py
  ├── get_domain_info()
  ├── check_domain_accessibility()
  └── get_domain_dns_info()
  ↓
format_domain_analysis() in formatter.py
  ↓
Telegram: Domain analysis
```

## Основные компоненты

### 🤖 Bot Core (`src/core/bot.py`)
- Управление состояниями (FSM - Finite State Machine)
- Обработчики команд для каждого типа поиска
- Интеграция со всеми OSINT модулями
- Отправка результатов в Telegram

### 🔍 OSINT Modules (`src/modules/osint/`)

#### scrapers.py
- `PlatformScraper` - базовый класс для парсинга
- `GitHubScraper` - парсинг GitHub профилей
- `TwitterScraper` - парсинг Twitter
- `InstagramScraper` - парсинг Instagram
- `LinkedInScraper` - парсинг LinkedIn
- `VKScraper` - парсинг VK
- `TelegramScraper` - парсинг Telegram

#### phone.py
- `check_phone_on_platform()` - проверка номера на платформе
- `extract_phone_info()` - извлечение информации
- `search_phone()` - основной поиск по номеру
- `search_phone_on_sites()` - поиск на поисковых сайтах

#### email.py
- `search_email()` - поиск email на платформах
- `get_email_domain_info()` - информация о домене email
- `search_email_breaches()` - проверка утечек данных

#### domain.py
- `is_valid_ip()` - проверка IP адреса
- `is_valid_domain()` - проверка домена
- `get_domain_info()` - основная информация о домене
- `check_domain_accessibility()` - проверка доступности
- `get_domain_dns_info()` - DNS информация
- `analyze_domain_complete()` - полный анализ

#### username.py
- `search_username()` - главная функция поиска (использует scrapers)

### 📝 Formatter (`src/utils/formatter.py`)
- `format_result()` - маршрутизатор к нужному форматтеру
- `format_username_detailed()` - красивый вывод username результатов
- `format_phone_results()` - форматирование phone результатов
- `format_email_results()` - форматирование email результатов
- `format_domain_analysis()` - форматирование domain результатов

### ⚙️ Settings (`config/settings.py`)
- Загрузка переменных окружения
- Конфигурация бота
- Хранение токенов и ключей

## Технологии

### Backend
- **Python 3.13** - язык программирования
- **aiogram 2.x** - Telegram Bot API
- **aiohttp** - асинхронные HTTP запросы
- **beautifulsoup4** - парсинг HTML
- **loguru** - логирование

### Data Processing
- **Парсинг HTML** - BeautifulSoup4
- **Асинхронность** - asyncio
- **Регулярные выражения** - re
- **JSON** - json (для работы с данными)

### Storage
- **Memory Storage** - FSM состояния в памяти
- **Local Files** - кеш и логи

## Безопасность

### ✅ Реализовано
- Валидация входных данных
- Обработка исключений
- Логирование всех операций
- Timeout для HTTP запросов (10-15 сек)
- User-Agent для имитации браузера

### 🔒 Рекомендации
- Использовать переменные окружения для токенов
- Регулярно обновлять зависимости
- Не хранить чувствительные данные в коде
- Соблюдать rate limiting для API

## Производительность

### Оптимизация
- Асинхронные запросы (aiohttp) вместо синхронных
- Параллельная обработка запросов (asyncio.gather)
- Timeout на запросы для предотвращения зависания
- Кеширование результатов (при необходимости)

### Масштабируемость
- Модульная архитектура - легко добавить новые платформы
- Асинхронность позволяет обрабатывать множество запросов
- Можно добавить Queue для очередей запросов

## Возможные улучшения

### 🚀 Планируемые
1. Интеграция с API платформ (требует API ключи)
2. Парсинг JavaScript-heavy сайтов (Selenium/Playwright)
3. Кеширование результатов в БД
4. Веб-интерфейс для результатов
5. Экспорт результатов (PDF, CSV)
6. Расширенный анализ (граф связей, дерево информации)
7. Machine Learning для анализа паттернов
8. WebScraper-прокси для скрытия IP

### 📚 Документация
- [OSINT_BOT_UPDATES.md](OSINT_BOT_UPDATES.md) - обновления
- [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - примеры использования
- [README.md](README.md) - основная информация

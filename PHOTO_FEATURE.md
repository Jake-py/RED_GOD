# 📸 Функция отправки фотографий в OSINT боте

## ✅ Что было сделано

### 1. Функция извлечения фотографий (`formatter.py`)

Создана функция `extract_images_from_result()` которая:
- Извлекает URL фотографий из результатов поиска
- Проверяет все ключи для изображений: `profile_image`, `avatar`, `picture`, `photo`, `image`
- Валидирует URL (только `http://` и `https://`)
- Избегает дубликатов (использует `set`)
- Работает для типов поиска: `username`, `phone`, `email`

**Сигнатура:**
```python
def extract_images_from_result(result: Dict[str, Any]) -> List[str]:
    """Extract image URLs from OSINT search result"""
```

**Пример использования:**
```python
from src.utils.formatter import extract_images_from_result

result = {...}  # Результат поиска
images = extract_images_from_result(result)  # List[str] - URLs фотографий
```

### 2. Обновление обработчиков в боте (`bot.py`)

Обновлены три обработчика сообщений:
- `process_username` - поиск по username
- `process_phone` - поиск по номеру телефона
- `process_email` - поиск по email адресу

**Логика отправки фотографий:**

1. Извлечение фотографий из результатов:
```python
images = extract_images_from_result(result)
```

2. Если есть фотографии:
   - **1 фотография:** отправляется одиночной фотографией
     ```python
     await bot.send_photo(message.chat.id, images[0])
     ```
   - **Несколько фотографий:** отправляются альбомом/группой
     ```python
     media_group = [types.InputMediaPhoto(media=img) for img in images[:10]]
     await bot.send_media_group(message.chat.id, media_group)
     ```

3. После фотографий отправляются текстовые результаты поиска

4. Все ошибки при отправке фотографий логируются на уровне `debug` и не прерывают отправку текстовых результатов

## 🎯 Преимущества

- **Лучший UX:** фотографии отправляются перед текстовыми результатами
- **Компактность:** несколько фотографий отправляются альбомом вместо отдельных сообщений (избегаем "каша-маша")
- **Надежность:** ошибки при отправке фотографий не влияют на отправку результатов
- **Гибкость:** автоматическая обработка разного количества фотографий

## 📊 Поддерживаемые платформы с фотографиями

Фотографии извлекаются с платформ, где они доступны:

### Username поиск:
- **GitHub** - profile_image (аватар пользователя)
- **Twitter** - profile_image (аватар профиля)
- **Instagram** - profile_image (аватар профиля)
- **LinkedIn** - profile_image (фото профиля)
- **VK (ВКонтакте)** - photo (фото профиля)
- **Telegram** - profile_image (аватар канала/пользователя)

### Phone поиск:
Обычно не возвращает фотографии, так как поиск основан на проверке присутствия номера в сервисах

### Email поиск:
Может возвращать фотографии профилей, если они найдены при поиске связанных аккаунтов

## 🧪 Проверка работоспособности

Все функции протестированы:

```
✓ Извлечение URL фотографий из структурированных результатов
✓ Отправка одной фотографии (single photo)
✓ Отправка нескольких фотографий (media group/album)
✓ Пропуск отправки если фотографий нет
✓ Валидация URL (фильтрация некорректных протоколов)
✓ Удаление дубликатов фотографий
✓ Обработка ошибок при отправке
```

## 📝 Примеры результатов

### 1 фотография найдена:
```
[Одна фотография отправлена как single photo]

🔍 Результаты поиска по username: red_ice
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 GitHub:
  Имя: Red Ice
  Аватар: https://avatars.githubusercontent.com/u/12345?v=4
  ...
```

### Несколько фотографий найдено:
```
[2 фотографии отправлены как альбом]

🔍 Результаты поиска по username: red_ice
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 GitHub: ...
📍 VK: ...
```

### Фотографии не найдены:
```
🔍 Результаты поиска по username: ghost_user
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 GitHub: Не найдено
📍 Twitter: Не найдено
...
```

## 🔧 Техническая информация

### Максимальное количество фотографий
- Telegram ограничивает альбом до 10 медиа-элементов
- Код отправляет максимум 10 фотографий: `images[:10]`

### Обработка ошибок
- Сетевые ошибки при отправке фотографий логируются как DEBUG
- Результаты поиска отправляются даже если фотографии не отправились
- Нет прерывания обработки запроса

### Поддерживаемые форматы
- JPEG (`.jpg`, `.jpeg`)
- PNG (`.png`)
- GIF (`.gif`)
- WebP (`.webp`)
- Telegram поддерживает все стандартные форматы

## 📄 Файлы были изменены

1. **`src/utils/formatter.py`**
   - ✓ Добавлена функция `extract_images_from_result()`

2. **`src/core/bot.py`**
   - ✓ Обновлены импорты (добавлен `extract_images_from_result`)
   - ✓ Обновлен handler `process_username` с логикой отправки фотографий
   - ✓ Обновлен handler `process_phone` с логикой отправки фотографий
   - ✓ Обновлен handler `process_email` с логикой отправки фотографий
   - ✓ Использован правильный синтаксис aiogram: `types.InputMediaPhoto(media=url)`

## 🚀 Итог

Функция отправки фотографий полностью интегрирована в OSINT бота и готова к использованию!

Теперь при поиске по username, phone или email:
- Найденные фотографии профилей отправляются первыми
- Несколько фотографий отправляются как альбом для чистоты UI
- Текстовые результаты отправляются после фотографий
- Ошибки при отправке фотографий не влияют на отправку результатов

**Отправляйте `/osint_username red_ice` и получайте фотографии профилей! 📸**
